extends Node2D

func _ready():
	VisualServer.set_default_clear_color(Color.orange)
# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called w
# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
